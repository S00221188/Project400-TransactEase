<Window x:Class="Project400_TransactEase.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:Project400_TransactEase"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="1200" WindowStartupLocation="CenterScreen" Background="#3D3D3D">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <!-- Top Bar -->
        <materialDesign:Card Grid.Row="0" Grid.ColumnSpan="3" Padding="10" Margin="10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Logout" Click="Logout_Click" Margin="10" Style="{StaticResource MaterialDesignRaisedButton}" Background="#C62828"/>
                <TextBlock Name="WelcomeText" FontSize="16" VerticalAlignment="Center" Margin="20,0" FontWeight="SemiBold"/>
                <Button Content="Settings" Click="Settings_Click" Margin="10" Style="{StaticResource MaterialDesignOutlinedButton}" Background="Yellow" />
                <TextBlock Name="DateTimeText" Margin="10" VerticalAlignment="Center" FontSize="14"/> 
            </StackPanel>
        </materialDesign:Card>

        <!-- Left Panel -->
        <!-- <Grid Grid.Row="1" Grid.Column="0" Background="#2E2E2E" Margin="10"> -->
        <materialDesign:Card Grid.Row="1" Grid.Column="0" Margin="10" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <!-- Bill List -->
                <StackPanel Grid.Row="0">
                    <TextBlock Text="Current Bill" FontWeight="Bold" FontSize="16" Foreground="#00F2FF" Margin="0,0,0,10"/>
                    <Border Background="#F2F2F2" CornerRadius="6" Padding="6">

                    <ListBox Name="BillList" Height="420" Style="{StaticResource MaterialDesignListBox}" MouseDoubleClick="BillList_MouseDoubleClick"/>
                    </Border>
                </StackPanel>
                <!-- Total & Action Buttons-->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
                    <TextBlock Text="Total: " FontWeight="Bold" FontSize="16"/>
                    <TextBlock Name="TotalText" FontSize="14" FontWeight="SemiBold" Margin="5,0,0,0" />
                </StackPanel>

                <UniformGrid Grid.Row="2" Columns="2" Rows="2" Margin="0,10,0,0">
                    <Button Content="Bill" Click="Bill_Click" Style="{StaticResource MaterialDesignRaisedButton}" Margin="5"/>
                    <Button Content="Clear" Click="Clear_Click" Style="{StaticResource MaterialDesignRaisedButton}" Margin="5"/>
                    <Button Content="Pay" Click="Pay_Click" Style="{StaticResource MaterialDesignRaisedButton}" Margin="5"/>
                    <Button Content="Tabs" Click="Tabs_Click" Style="{StaticResource MaterialDesignRaisedButton}" Margin="5"/>
                </UniformGrid>
            </Grid>
        </materialDesign:Card>

        <!-- Product Panel -->
        <materialDesign:Card Grid.Row="1" Grid.Column="1" Margin="10" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
        <WrapPanel Name="ProductPanel" ItemWidth="150" ItemHeight="90"/>
        </ScrollViewer>
        </materialDesign:Card>
        <!-- Navigation Bar -->
        <materialDesign:Card Grid.Row="2" Grid.Column="1" Margin="10">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Home" Click="Filter_Click" Tag="All" Margin="5" Style="{StaticResource MaterialDesignOutlinedButton}"/>
            <Button Content="Draft" Click="Filter_Click" Tag="Draft" Margin="5" Style="{StaticResource MaterialDesignOutlinedButton}"/>
            <Button Content="Bottle" Click="Filter_Click" Tag="Bottle" Margin="5" Style="{StaticResource MaterialDesignOutlinedButton}"/>
            <Button Content="Shot" Click="Filter_Click" Tag="Shot" Margin="5" Style="{StaticResource MaterialDesignOutlinedButton}"/>
            <Button Content="Cocktail" Click="Filter_Click" Tag="Cocktail" Margin="5" Style="{StaticResource MaterialDesignOutlinedButton}"/>
            <Button Content="Soft Drink" Click="Filter_Click" Tag="Soft Drink" Margin="5" Style="{StaticResource MaterialDesignOutlinedButton}"/>
            <Button Content="Snack" Click="Filter_Click" Tag="Snack" Margin="5" Style="{StaticResource MaterialDesignOutlinedButton}"/>
        </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>
